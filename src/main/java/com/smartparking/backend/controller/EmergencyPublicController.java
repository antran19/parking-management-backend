package com.smartparking.backend.controller;

import com.smartparking.backend.service.EmergencyService;
import org.springframework.web.bind.annotation.*;

/**
 * EmergencyPublicController — API SOS công khai (Thiên phụ trách)
 *
 * TODO (Thiên): Implement endpoint:
 * - GET /public/emergency/status → Trạng thái SOS hiện tại (không cần login)
 */
@RestController
@RequestMapping("/api/v1/public/emergency")
public class EmergencyPublicController {

    // TODO: Inject EmergencyService

    // TODO: Implement endpoint
}
